# Play-by-play

Available on https://blast-cs.onrender.com

![Showcase](./showcase.gif)

## Running locally

Install dependencies with `pnpm i` in the root folder
Run the app with

```
pnpm dev
```

The next app will be available `localhost:3000` the API on `localhost:10000`
